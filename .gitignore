# wrangler writes account cache here on every deploy
.wrangler/

# deploy output, rebuilt from index.html each time
dist/

# local scratch
*.log

# marketing drafts, kept out of the repo because the site is served from the repo root
promo/
